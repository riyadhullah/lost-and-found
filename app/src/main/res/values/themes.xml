<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LostAndFound" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blue_aiub</item>
        <item name="colorOnPrimary">@color/white</item> <!-- title, icons -->
        <item name="colorSecondary">@color/yellow_aiub</item>
        <item name="android:windowLightStatusBar">true</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LostAndFound" parent="Base.Theme.LostAndFound" />

    <style name="Bottom.Selected" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/yellow_aiub</item>
    </style>

</resources>